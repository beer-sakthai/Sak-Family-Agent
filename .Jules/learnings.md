## 2026-08-25 - Removing Unused Imports in Python Files\n**Learning:** Running Ruff formatter and linter (`ruff check --fix`) successfully catches and removes unused Python imports and auto-upgrades syntax (like `Optional[str]` to `str | None`). When cleaning files, we can confidently run test suites to assert stability.\n**Action:** Run `uv run ruff check --fix` to perform clean-up sweeps without breaking syntax or typings, and run `uv run pytest` to verify.
Learned how to use vitest to mock internal modules inside next.js api route handlers testing, in particular selfHealingDashboardEngine via vi.mock.
