Same as `saksit-free-image-generation/references/env-key-discovery.md` —
the keys `GOOGLE_API_KEY`, `OPENROUTER_API_KEY`, `MCP_COMPOSIO_API_KEY`
are in `/opt/data/profiles/saksit/.env` and `/opt/data/.env`.
