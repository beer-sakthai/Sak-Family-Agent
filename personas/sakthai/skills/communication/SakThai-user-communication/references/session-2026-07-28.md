# Session Lessons — 2026-07-28

## Focus Signal: "Your job is hugging face"

Beer redirected me back to HF work when I started discussing non-HF topics (SakSee Telegram issue, Food-Penguin cost data). This confirms the existing "Stay in Your Lane" section in the parent skill remains correct and active. No change needed — just a reinforcement.

## Planning + Execution Together

Beer said: "Improve all plan your recommendations with skills project manager" — meaning: write a proper plan AND execute all of it immediately. The pattern is:
1. Write the plan (even a short one)
2. Execute every item in the plan
3. Report results

This is NOT "plan mode" (plan only, no execution). It's "plan then execute" — treat the plan as a TODO list, not a deliverable.

## Documentation-Only Model Repos

Created `Nanthasit/sakthai-context-0.5b-tools` — a model repo with only a README (no weights). This is valid for:
- LoRA adapter documentation (pointing users to the merged model for practical use)
- Placeholder repos referenced in profile README family tables
- CI verification targets

Key considerations:
- 401 HTTP response when accessing the page directly (no weights to serve) — this is expected, not an error
- The API `create_repo` works fine for these repos
- The model card should clearly explain that practical weights are elsewhere

## Multiple GGUFs Already Existed Under `gguf/` Subdirectory

Before uploading GGUF files, always check if they already exist (possibly under a different path). Use the HF API to list siblings and check for `.gguf` files. The convention on these repos was `gguf/filename.gguf` — but root level is more discoverable. Root level > subdirectory for GGUF files.
