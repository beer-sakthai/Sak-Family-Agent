# Session reference: sakthai-plus-1.5b-lora health check (2026-07-30)
# LoRA adapter on Xet storage — same-day metrics, zero downloads

model: Nanthasit/sakthai-plus-1.5b-lora
date: 2026-07-30
type: LoRA adapter (PEFT) — Qwen/Qwen2.5-1.5B-Instruct
backend: Xet (Content-Length returned actual sizes, not x-linked-size)

## LoRA Adapter Detection

Both `safetensors` and `gguf` top-level keys absent from `/api/models/{id}`.
Indicator files: `adapter_model.safetensors`, `adapter_config.json`, `training_args.bin`.

```python
has_safetensors_key = 'safetensors' in api_data     # False
has_gguf_key = 'gguf' in api_data                    # False
has_adapter_file = 'adapter_model.safetensors' in siblings  # True
```

## Storage Discrepancy

- `usedStorage` (API): 750,538,597 bytes (715.77 MB)
- Sum of current HEAD-request file sizes: ~85 MB (81.4 MB)
- Discrepancy: 8.8× — Git history/LFS pointer overhead from multiple commits
- This is normal for Xet-backed repos with repeated uploads

## Sibling HEAD Requests (no x-linked-size needed)

Unlike the coder-1.5b GGUF model, this repo returned correct sizes via `Content-Length`:

| File | Size |
|------|------|
| adapter_model.safetensors | 70.5 MB |
| tokenizer.json | 10.9 MB |
| README.md | 6 KB |
| adapter_config.json | 1 KB |
| training_args.bin | 5 KB |
| chat_template.jinja | 2 KB |

## Same-Day Metrics

- `createdAt`: 2026-07-30T13:02:41Z
- `lastModified`: 2026-07-30T20:53:44Z
- Age at eval: 7.9 hours
- `age_days` formula: `max(0, 1)` = 1 → velocity = 0/1 = 0.0
- Report: alert "Not yet meaningful — model is <24 hours old"

## Sibling Comparison (rank 18/21)

- 21 total models by Nanthasit
- Same-day zero-download peers: 8 models (context variants, combined-v8, plus variants)
- Top performer: sakthai-context-1.5b-merged (1599 downloads)

## Upload

- Used `huggingface_hub.upload_file()` with stable path (overwrite)
- huggingface_hub v1.x reports "No files modified" but actually commits the new content
- Use `commit_url` attribute (not `commit_sha` or `commit_sha_in_pr`) from CommitInfo
