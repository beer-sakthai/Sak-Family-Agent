# plus-1.5b Health Eval — 2026-07-30 Cron Run

## Summary

Standard free-model health eval for `Nanthasit/sakthai-plus-1.5b`. Created same day (8.8h old at scan time). Zero downloads, zero likes. Model is a fresh Qwen2.5-1.5B-Instruct fine-tune for tool-calling.

## Key facts

- `usedStorage` = 6,186,356,180 bytes (6.19 GB) — available from base API endpoint
- `safetensors` key present: BF16 params = 1,543,714,304 (1.54B)
- Individual sibling sizes all 0 via `?expand[]=siblings` — fresh-model indexing delay
- No `createdAt` in single-model response; `_id` MongoDB ObjectId used as fallback
- 9 stale health-check files already in `.eval_results/`

## Pitfall confirmed

**`?expand[]=siblings` returns 0-byte sizes for same-day models.** Even with `?blobs=true&expand[]=siblings`, all 19 siblings showed `size: 0`. The `usedStorage` field is available from the base endpoint and represents the real stored byte count. For same-day models, trust `usedStorage` over individual sibling sizes.

## Verification trap

Skipped creating a `hermes-verify-*` temp file (used inline `python3 << 'PYEOF'` instead). System flagged paths as unverified and required a second round trip. The skill was updated to document this requirement.

## Upload

Single path uploaded: `.eval_results/health-check-plus-1.5b-2026-07-30.yaml`. Verified via `curl -sL` raw endpoint (HTTP 200).
