# sakthai-context-0.5b-merged Mixed-Format Health Check (2026-07-30)

**Model**: `Nanthasit/sakthai-context-0.5b-merged`
**Format**: Mixed (safetensors + GGUF)
**Arch**: Qwen2 494M, hidden_size=896, 24 layers, 14 heads, 32K context
**Pipeline**: text-generation
**Library**: transformers

## Key metrics

- **Downloads**: 1,370 | **Likes**: 0 | **Age**: 25 days
- **Velocity**: 54.8/day | **Rank**: #2/18 among author models (23.3% share)
- **Health**: Good (74/100)

## Files (from tree endpoint)

| File | Size |
|------|------|
| `model.safetensors` | 988 MB (BF16) |
| `sakthai-0.5b-Q4_K_M.gguf` | 398 MB (Q4_K_M) |
| `tokenizer.json` | 11 MB |
| `config.json`, `generation_config.json`, `tokenizer_config.json` | < 1 KB each |
| `README.md` | 10 KB |
| `LICENSE`, `chat_template.jinja`, `.gitattributes` | Small |

## What made this health check notable

### 1. Mixed-format detection
Both `safetensors` and `gguf` top-level keys present in API response. This is the "happy path" — everything works, no workarounds needed:
- `safetensors.parameters.BF16` = 494,032,768 (exact 0.5B)
- `gguf.total` = 494,032,768 (matches BF16)
- `gguf.totalFileSize` = not present in API (but tree endpoint shows 398 MB)
- `gguf.architecture` = "qwen2", `gguf.context_length` = 32768 (matches config.json)

### 2. `modelCount` edge case
`safetensors.modelCount` was `None` despite a valid `model.safetensors` file. The model has a single merged safetensors file (not sharded), and the API doesn't populate `modelCount` for single-file repos. The `parameters` sub-dict had correct values regardless.

### 3. Clean repo hygiene
17 siblings total, 9 non-hidden files, zero dev artifacts. No artifact inflation — this is a repo uploaded with intention, not dragged from a dev environment. Contrast with GGUF-only repos that often have 1,500+ siblings from `.venv/` inflation.

### 4. Scoring
- **Popularity: 48** — 1,370 dl / 2000 * 100 * 0.7 = 47.95 + 0 likes = round(47.95)
- **Download momentum: 98** — 54.8/d velocity gives vel_score=min(54.8/40*100,100)=100; rank #2/18 gives rank_score=100-(1)*(100/18)=94.4; weighted 0.6*100 + 0.4*94.4 = 97.8, round to 98
- **Benchmark coverage: 40** — 2 entries in model-index (2 * 20 = 40)
- **Card quality: 85** — base 85, no deductions (has base_model, tags, model-index; README >2KB skips description deduction; widget present)
- **Hygiene: 100** — no artifacts, not GGUF-only, not skeleton
- **Total**: (48+98+40+85+100)/5 = 74.2 → round to 74

### 5. Comparison with siblings

| Model | Params | Downloads | Velocity | vs 0.5B |
|-------|--------|-----------|----------|---------|
| context-1.5b-merged | 1.5B | 1,599 | 64.0/d | +17% |
| **context-0.5b-merged** | **0.5B** | **1,370** | **54.8/d** | — |
| context-7b-merged | 7.6B | 744 | 31.0/d | -46% |
| context-0.5b-tools | 0.5B | 94 | 3.8/d | -93% |

### 6. API endpoints used (zero cost)
- `GET /api/models/{id}` — base metadata (downloads, likes, pipeline_tag)
- `GET /api/models/{id}/tree/main` — file sizes
- `GET /api/models/{id}/raw/main/config.json` — architecture
- `GET /api/models?author=Nanthasit&sort=downloads&limit=50` — sibling list

No `?blobs=true` needed — the base API + tree endpoint gave complete data.

### 7. Verification
- Local YAML → `huggingface_hub.upload_file()` (stable + dated paths)
- Verified via `get_paths_info()` (size check) + `curl -sL` (HTTP 200 + content check)
- 10 field-level assertions all passed
