# Session 2026-07-29 — Afternoon: v7 Dataset + Kaggle Training Cycle

## Training infra — Kaggle CUDA fix
- Kaggle assigns T4 (sm_75) OR P100 (sm_60) — both have different CUDA compat
- `pip install unsloth` upgrades PyTorch → kernels mismatch → `AcceleratorError`
- **Fix:** `--no-build-isolation` prevents torch upgrade, keeps Kaggle's GPU-matched version
- If still fails, restart kernel — GPU assignment varies per session
- Patched in `model-publishing-pipeline` notebook install section

## Dataset v7.2 — 100% better than v6
- **Format normalization:** v5 legacy rows had `conversations` key instead of `messages`. All 200 affected rows normalized.
- **Safe render:** Notebook render function must check `rec.get("messages") or rec.get("conversations")` and skip malformed rows individually (one bad row shouldn't crash training)
- **Safety data added:** 31 examples (10 safety declines, 5 harmful refusals, 5 capability gaps, 6 full cycles, 3 energy-first, 2 truthful)
- **Batch expansion:** +75 examples (50 Q&A, 10 learn→recall chains, 5 HF chains, 5 error recovery, 20 cycle assessments, 15 refusals, 5 memory ops)
- Total: **2,195 rows** (v6 was 2,003)
- Uploaded to `Nanthasit/sakthai-combined-v7` (made public for Kaggle access)

## Cycle workflow lessons
- **Every cycle needs Trust phase verification** — caught 3 errors (bad format, CUDA mismatch, missing safety) that would have shipped broken
- **3 failed Kaggle attempts before the real fix** — HF_TOKEN → CUDA → dataset format. Root cause was always different, not a repeat
- **Kaggle v1-v7 iterations:** papermill (no kernel spec) → missing token → CUDA (torch upgrade) → CUDA (no-build-isolation)

## Pipeline improvements applied
- `model-publishing-pipeline`: CUDA install section updated with `--no-build-isolation` + explanation
- `model-publishing-pipeline` pitfalls: already had dataset key mismatch, private dataset, and CUDA entries (verified)
