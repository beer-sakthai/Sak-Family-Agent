# Sak Family Agent — Master Plan Index

> **Dream → Hope → Care → Joy → Trust → Growth**

This is the master index. Navigate from here. Always update sub-plans in-place;
never duplicate content across files.

---

## 🗂️ Where Things Live

| What you're looking for | Where to find it |
|---|---|
| **Business strategy & monetization plan** | [`product/PLAN.md`](./product/PLAN.md) |
| **Session notes & brainstorms** | [`product/sessions/`](./product/sessions/) |
| **Team identity, Charge & Principles** | [`docs/SOUL.md`](./docs/SOUL.md) |
| **Memory tools & agent guide** | [`docs/SAKTHAI.md`](./docs/SAKTHAI.md) |
| **Beer's profile & core values** | [`docs/USER.md`](./docs/USER.md) |
| **Agent operating rules** | [`docs/OPERATING_CONTRACT.md`](./docs/OPERATING_CONTRACT.md) |
| **Architecture & capabilities** | [`docs/architecture.md`](./docs/architecture.md) · [`docs/capabilities.md`](./docs/capabilities.md) |
| **Python source** | [`personas/sakthai/sakthai/`](./personas/sakthai/sakthai/) — the installed package |
| **Agent personas** | [`personas/`](./personas/) |
| **Skills** | [`personas/*/skills/`](./personas/) (per-persona overlays) · [`personas/shared/skills/`](./personas/shared/skills/) · [`library/`](./library/) (31 curated) |
| **Helper scripts** | [`scripts/`](./scripts/) |
| **Tests** | [`tests/`](./tests/) |
| **Web dashboard** | [`apps/sak_agent_dashboard/`](./apps/sak_agent_dashboard/) — Next.js frontend; HTTP API in [`personas/sakthai/sakthai/web/`](./personas/sakthai/sakthai/web/) |
| **Assets / images** | `assets/` — on disk but untracked; see [`docs/repo-audit-2026-08-08.md`](./docs/repo-audit-2026-08-08.md) |

---

## 🚦 Current Status

| Area | Status |
|---|---|
| Repository hygiene — persona SOULs | ✅ All 6 personas done (2026-07-02) |
| Business strategy — market analysis | ✅ Done (2026-07-02) |
| MVP definition | ✅ Done (2026-07-02) — ServiceQuoteBot |
| Monetization strategy | ✅ Done (2026-07-02) — Setup + Subscription |
| MVP execution — ServiceQuoteBot build | ✅ Done (2026-07-02) |
| Model Evaluation — Task validation via lm-evaluation-harness | ✅ Done (2026-07-06) |
| Documentation — Revamp README with banner and detailed status | ✅ Done (2026-07-06) |
| Repository hygiene — per-agent skill name prefixes; SakFin role folded into SakTan's `SOUL.md` (no standalone SakFin persona) | ✅ Done (2026-07-06) |
| Restructure — copy sakthai package + agent-self-evolution into each persona; remove root packages/ and sakthai/ | ✅ Done (2026-07-06) |
| Build/CI repointing — canonical package at personas/sakthai/sakthai; pyproject, workflows, Dockerfile, path fixes | ✅ Done (2026-07-06) |
| Restructure — move servicequotebot scaffold from personas/ to services/ | ✅ Done (2026-07-06) |
| **Plans audit & refresh** — standardized markers, cross-links, archived completed sub-plans | ✅ Done (2026-07-07) |
| **Test Coverage Improvement** — 100% coverage on all previously untested modules, sync and eval Edge Cases | ✅ Done (2026-07-08) |
| **Security hardening + free-local model policy** — memory metadata redaction, static-serving path canonicalisation, all agents default to local `sakthai`, handles standardized (PR #344) | ✅ Done (2026-07-10) |
| **Test Coverage round 2** — dashboard data tests + injectable store, guardrail deny-path branches, MCP guardrail paths, auth expiry, store aggregates, small CLI/web/config gaps, guardrails added to mutation scope; coverage 95.4% → 98.3%, floor raised to 95 | ✅ Done (2026-07-10) |
| **README rewrite** — corrected origin story (fabricated "cleaners found me" detail removed at Beer's request), status bars + emoji, refreshed project facts | ✅ Done (2026-07-10) |
| **Repo security audit round 2** — gitleaks CI gate added (`secret-scan.yml`), pip-audit dependency audit added, `@latest` action pinned, dead Dependabot npm target fixed, HF_TOKEN moved to env block, SECURITY.md/CLAUDE.md corrected; report at `docs/security_audit_2026-07-11.md` | ✅ Done (2026-07-11) |
| **Caveman skill injection in prompt_builder** — replaced the placeholder in `render_skills_prompt_block` with real caveman skill loading (mirrors `agent/loop.py`), simplified directive kept as fallback | ✅ Done (2026-07-11) |
| **Test Coverage round 3** — guardrails wrapper/find bypass branches (88% → 99%, zero missed statements), agent-loop failure seams, `memory pull` CLI, tool overrides, telegram session/main paths, provider detection edges, win32 CLI stream reconfigure; coverage 97.6% → 99.2%, floor raised to 97, CLAUDE.md floor claim corrected | ✅ Done (2026-07-12) |
| **SOUL consistency + repo hygiene** — fix six-persona SOUL drift (sibling lists in SakThai/SakKing, SakSee model line → local `sakthai` per PR #344 policy, finance lane → SakTan/SakFin, SakKing phantom tools + web-lane split vs SakSee, SakJules/SakSee craft passes), refresh `personas/README.md` skill counts from disk, add `tests/test_soul_consistency.py` CI guard, untrack `coverage.xml`/`sakking-dashboard.tar.gz`, remove throwaway `LABELER_TEST.md` | ✅ Done (2026-07-13) |
| **Security audit round 3** — git-URL transport validation (`giturl.py`, applied in memory sync + extensions install), `extensions remove` containment check, HTTP sync timeout, Telegram `/workflow` empty-allowlist bypass, bot handlers moved off the event loop, tool-override load failures logged | ✅ Done (2026-07-13) |
| **Skill-path fix + dry-run skill preflight** — added `personas/shared/skills/` to `default_skill_roots()` so `Sak-auto-cycle-loop` resolves; `--dry-run` now validates `--with-skills` names (fails on misses, reports resolved), live path warns instead of silently dropping; +6 tests, verified end-to-end | ✅ Done (2026-07-13) |
| **Sentinel relative-path hardening (PR #378/#381)** — `_is_sensitive_path` blocks relative paths to sensitive data (`.ssh/`, `.aws/`, shell histories, key basenames) via `_SENSITIVE_BASENAMES`/`_SENSITIVE_DIRS`/`_SENSITIVE_KEY_STEMS`, case-insensitively and across separator values, backup-suffixed keys, globs, and interpreter one-liners; superset of the concurrently-merged #379; synced across all six personas; prevention: `tests/test_persona_guardrails_parity.py` fails CI on persona guardrail drift + regression tests in `tests/test_sentinel_ssh_leak.py` | ✅ Done (2026-07-14) |
| **Relative system-root blocking (re-land of PR #380)** — `_is_sensitive_path` now treats relative paths whose first component names a critical root (`etc/passwd`, `var/log/…`) as sensitive, with a single-component `tmp` exception; `.config`/`.npm` added to `_SENSITIVE_DIRS`, `credentials` to `_SENSITIVE_BASENAMES`; landed as a delta on top of the stronger #381 hardening instead of merging the conflicting/regressive #380 branch; synced across all six personas; regression tests in `tests/test_guardrails_relative_roots.py` | ✅ Done (2026-07-14) |
| **Branch consolidation — all 8 open PRs merged to main** — dependabot #386 (mypy 2.3.0) + #387 (actions group); Sentinel #384 (shell-config basenames + critical roots in `_SENSITIVE_NAME_RE`), #385 (ssh/ssh-add/ssh-keygen/ssh-copy-id in scan lists + `tests/test_sentinel_ssh_tools.py`), #388 (`cp` check widened from kubectl-only to docker/podman/kubectl), #389 (docker/podman/kubectl/chroot/nsenter added to destructive + exfiltration scan lists), #391 (`,` added as `_is_sensitive_path` delimiter + `tests/test_guardrails_sentinel_bypasses.py` case), #392 (protections already subsumed by the consolidated version; merged for history, journal entry kept). Conflicting hunks resolved by keeping the stronger consolidated implementation (chroot NEWROOT check, conservative nsenter flag list, no internal-command censoring); guardrails synced across all six personas; closed branches #378/#380 skipped as previously superseded by #381/#382 | ✅ Done (2026-07-16) |
| **Security Audit & High-Priority Fixes Plan** — comprehensive audit completed (audit report: 120+ pages, A+ grade); two high-priority fixes identified: (1) Rotate Stripe + Twilio credentials in git history (5 min, Beer), (2) Add web API bearer token auth (4-6 hrs, Claude Code); full plan at `security/SECURITY_FIXES_PLAN.md` with phases, tests, rollout, and rollback. Additional: Upgraded vulnerable pinned dependency `cryptography` from v49.0.0 to v50.0.0 (CVE-PYSEC-2026-3552). | ✅ Done (2026-08-09) |
| **Native per-persona capabilities (no Hermes dependency)** — persona-aware skill discovery (fixes `SKILLS_DIR` hardcoded to SakThai in `config.py`), auto-wired per-persona `mcp.json` + model/provider defaults for `run`/`chat --persona`, Telegram gateway `SAKTHAI_PERSONA` support, removal of the dead `sakthai-telegram-native@.service` template, `CLAUDE.md` canonical-package claim corrected; full test suite + mypy + ruff green | ✅ Done (2026-08-02) |
| **HF Ecosystem Improvement** — 5-phase plan written: broken/skeleton repo fixes, card/metadata consistency (19/16/7 counts), functional upgrades, cleanup, automation + health monitoring (cron confirmed dead); plan at `docs/hf-cards-improvement-plan.md` | [/] In progress — awaiting approval to start Phase 1 |
| **Hermes profile scaffold repair (×6 agents)** — merge `6992353b` had silently deleted `infra/hermes-agents/{default,profiles/*}` and reverted `diagnose_personas.py`/`export_agent_repo.py`'s default-profile check back to the pre-`debd90de` sakking-keyed logic; restored 11 files from `debd90de`, fixed both regressed checks back to sakthai-keyed. `scripts/diagnose_personas.py` now passes 6/6 hermes-profile-scaffold checks (run with `.venv/bin/python3`, not system `python3` — the MCP-load subcheck needs the venv's `sakthai` import) | [/] In progress — uncommitted, awaiting Beer's go-ahead to commit |
| **Branch consolidation round 2 — repo reduced to `main` only** — 10 stale branches / 7 open PRs collapsed into one merge. Merged: #555 (MS Graph credentials added to the redaction sets across all persona `config.py`/`guardrails.py` + `sakthai-chat-cli`). Re-landed as a clean delta: #547's `make` guardrail (recipe scanning, `-C`/`-f` resolution, include-cycle guard) — its own branch had reverted `executor.py` hardening and deleted tracked `.workflow_runs/` fixtures during a bad merge, so only the guardrail hunks were taken. Consolidated: the unique protections from the five duplicate `_validate_filepath` rewrites (#552/#553/#554/#556/#557) folded onto main's #558 implementation — backslash-normalized traversal check, `opt` root, `.docker`/`.kube`/`.gnupg`/`.gcloud`/`.azure` dirs, `.env-`/`.env_`/`memory.db-` prefixes, private-key stem matching. Dropped without merging: two GraphClient branches that would have regressed #549's custom-scheme check back to a `startswith("http")` test, one SSRF branch already subsumed by #558, and a branch downgrading every GitHub Action from v7 to v4 | ✅ Done (2026-08-08) |
| **CLAUDE.md refresh — reconciled with the repo on disk** — re-audited every claim against the tree: persona package layout (three personas now carry partial real `sakthai/` dirs shadowing a `sakthai~origin_main` symlink, not five clean symlinks), the undocumented `agent/` modules (`guardrails`, `guardrails_hardened`, `security_hardening`, `context_filter`, `context_manager`, `prompt_builder`, `chat`, `eval`) and top-level `giturl`/`hf`/`sakking_skills`/`lead/`/`learn/ingest`, the real CLI surface (`chat`, `web setup|regen-token`, `extensions install` not `add`, `sessions clean` not `export`, `eval summary`, full `memory` subcommand list), 15 GitHub workflows (was 9), the mypy `sakthai.telegram.*` strict exemption, the closed web-server static-auth gap, and the full env-var set. Figures re-verified locally: 1,978 tests over 95 files green, 96.56% branch coverage, mypy strict clean over 69 files, ruff clean | ✅ Done (2026-08-08) |
| **README refresh — metrics reconciled with the repo** — replaced stale/unverifiable figures with locally verified ones (1,978 tests over 95 files, 96.56% branch coverage against the real `fail_under = 96`, mypy strict clean over 69 files, bandit 0 findings); corrected the package tree (14 tools not 10, added `security_hardening`/`guardrails_hardened`/`context_filter`/`context_manager`/`prompt_builder`/`chat` and the `cycle`/`web`/`dashboard`/`telegram`/`extensions`/`learn`/`lead` subpackages); persona skill counts recounted from `SKILL.md` files (823, was 843) with each persona's configured default model; provider table rewritten around how providers are actually selected (default model `claude-opus-4-8`); CI section rebuilt from the 15 real workflows, splitting per-push from scheduled; dropped the unverifiable security perf/benchmark tables in favour of the 8 named defense classes and measured hardening-test coverage; fixed `sakthai run` usage (takes a TASK argument) and the license section (all rights reserved) | ✅ Done (2026-08-08) |
| **Test coverage audit (analysis only)** — full suite re-measured on 3.11 (2,004 passed / 2 skipped / 6 deselected, 101 files, 96.018% branch coverage against `fail_under = 96`); eight ranked findings written up in [`docs/test-coverage-audit-2026-08-26.md`](./docs/test-coverage-audit-2026-08-26.md). Headline gaps: `telegram/bot.py` at 38% and hidden by a coverage `omit` with its authorization branch untested; `guardrails_hardened.py` + `security_hardening.py` (371 stmts) imported by nothing but their own test; `guardrails.py` section-6 container rules unreachable because `_check_destructive_tokens` denies first; web-auth tests cover acceptance only; the `SAKTHAI_*` deployment env readers at 0%; `team/engine.py` parallel failure paths untested; 13.5k lines of `personas/shared/sakthai/` (SakJules, SakTan) unmeasured. No production code changed | ✅ Done (2026-08-26) |
| **Test coverage audit follow-on (analysis only)** — re-measured on `main` at `a704a40` with the 2026-08-26 audit's exact command (2,251 passed / 2 skipped / 6 deselected / 124 subtests, 106 files): **95.849% branch coverage against `fail_under = 96`**, and CI measures **95.88%** on the same code — the floor is missed in both environments. **The headline finding is that it does not matter: the coverage floor prints `FAIL Required test coverage of 96.0% not reached` in CI and the step still reports `conclusion: success`** (verified on PR #1200's head `e9fca70`, both the 3.11 and 3.12 jobs), while the identical command exits 1 locally under pytest-cov 7.1.0. The mechanism is not yet identified and should be reproduced before it is diagnosed; the consequence is that the 96% floor has been decorative, which is why the previous audit's 0.169pp decline produced no CI signal. Six findings in [`docs/test-coverage-audit-2026-08-27.md`](./docs/test-coverage-audit-2026-08-27.md). Also new: `fail_under` is a package-wide average with no per-file or diff floor; the `client/` onboarding subsystem merged in #1198 (`cli/client.py` 87.6%, `client/manager.py` 89.5%, `client/verifier.py` 88.3%, `client/models.py` 93.4%) contributes 45 uncovered units, all error/exit and interactive-prompt paths; `sakthai-chat-cli/`'s 72 test files are run by no workflow (only bandit/codeql scan the tree); and two permission-branching tests self-skip under root, which accounts for the small local-vs-CI difference. Carried and re-verified unchanged: `guardrails.py` 89% with the container rules at 866–910 unreachable behind `_check_destructive_tokens`, `telegram/bot.py` hidden by `omit`, the web-auth rejection paths, `team/engine.py` parallel failures. Every package file except the omitted `telegram/bot.py` is measured — no dark modules. No production code changed | ✅ Done (2026-08-27) |
| **Test coverage audit round 3 (analysis only)** — re-measured on `main` at `1ebe527` with the previous audits' exact command: **95.862% branch coverage**, 213 uncovered statements and 170 partial branches — **identical miss/partial counts to 2026-08-27 across 76 commits**, because all of that work landed in `apps/`, docs and workflow config, and because nothing pushes back. Nine findings in [`docs/test-coverage-audit-2026-08-28.md`](./docs/test-coverage-audit-2026-08-28.md). **Finding 1 closed as an investigation:** the pytest-subtests hypothesis is dead (it is not a dependency and not installed; the "124 subtests" are `unittest.subTest` in `tests/test_persona_guardrails_parity.py`, which pytest 9 reports natively), and the freshness-audit row above already records the real state — `--cov-fail-under=96` is a one-line `ci.yml` edit nobody has landed. **New this round:** nine `SAKTHAI_*`/`MS_GRAPH_*` env vars have zero test references and `config.py`'s uncovered statements (`282-283, 288-289, 294, 299, 318-319`) are exactly the five deployment readers behind them, the ones `infra/vm-agents/env-templates/*.env.example` sets; `tests/test_persona_guardrails_parity.py` pins one file, while `web/server.py` — the bearer-token auth, subject of this week's #1214/#1219 cookie autofixes — is identical across five persona copies with nothing keeping it that way, and the suite imports only the canonical copy; an AST pass over all 1,886 `def test_*` finds six with no assertion at all, four of them the `initialize_hardened_guardrails` level tests and `test_detects_dangerous_symlinks`, all in the 371 statements no production code imports; `make mutation` is `|| true`-swallowed and in no workflow, so there is no assertion-strength signal either; and `services/teams-copilot-mcp/tests/` is a second unrun tree beside `sakthai-chat-cli/`, now 86 test files (was 72). **Upgraded from inference to proof:** `guardrails.py` section 6 is unreachable — all eight container forms are denied by the generic scanner at ~667, and the reason strings prove none reaches 866–910. Web auth is sharper than "rejection paths": of three credential channels (header, query, cookie) only the header is exercised at the HTTP boundary. No production code changed | ✅ Done (2026-08-28) |

| **Bandit remediation across the non-package trees** — triage of the Bandit backlog on the code-scanning dashboard, rebuilt onto current `main`. **`B615` ×23 (unpinned Hugging Face downloads), split by who owns the repo:** four upstream `Qwen/*` base models pinned to immutable commit SHAs resolved from the Hub API and verified to exist, with `BASE_MODEL_REVISION` falling back to `None` when `BASE_MODEL` is overridden so a pin can never outlive the model it names; own-namespace `Nanthasit/*` adapters and datasets given `# nosec B615` with the reason, since an eval/export script exists to exercise whatever was last pushed and a pin would freeze the training loop against its own output. Bandit's acceptance of a *variable* `revision=` was probed before editing — which is why the own-namespace calls did **not** get a variable resolving to `None`, a move that would silence the rule while changing nothing. **`B108` ×11** replaced hardcoded `/tmp` with `tempfile.mkdtemp()` (mode 0700) / `gettempdir()`, the actual win being a private per-run directory instead of a predictable name in a world-writable one. **`B110`/`B112`** narrowed rather than suppressed: four bare `except:` in `scripts/workbench/` were swallowing `KeyboardInterrupt`/`SystemExit` alongside the JSON error they meant to catch; both `agent-self-evolution/.../skill_module.py` copies narrowed identically and `diff`-verified. **`B105`/`B310`** suppressed with rationale at the call site — token *paths* read as passwords, and literal loopback / fixed-host URLs. Verified with bandit 1.9.4: the scoped scan and a bare `bandit -r .` agree exactly. `SECURITY-INSIGHTS.yml` added (OpenSSF Security Insights v2.0.0, validated against the upstream CUE schema), populated only from facts the repository can prove and deliberately omitting the `release` block, since no official distribution point exists for the package itself | [x] 2026-08-26 |
| **Scorecard Pinned-Dependencies — closed the remaining unpinned installs** — follow-on to the `scorecard.yml` restore in #1183, which fixed *why* the 41 alerts were stuck (no newer analysis was running to retire findings against files `b330e2f` had already deleted) but left the things the rerun re-scans still unpinned. `pipx install uv` → the SHA-pinned `astral-sh/setup-uv` already used by four other workflows (`ci.yml`, `run-evals.yml`); `pip install pylint` → hash-locked `.github/pylint-requirements.in`/`.lock` on bandit.yml's pattern, and the unpinned `pip install --upgrade pip` alongside it dropped; both Dockerfile base images pinned by digest with a Dependabot `docker` ecosystem added so the digests stay current; `scripts/gen_hash_lock.py` restored — `bandit-requirements.lock`'s own header names it as the way to regenerate, and it was a dangling reference. Branch-Protection and Code-Review are repository *settings*, not repository content, and stay open until Beer changes them | [x] 2026-08-26 |
| **Sak Agent Dashboard — unified system (Agents · Workflows · Analytics · CI)** — the three trees (`personas/sakthai/sakthai`, `apps/sak_agent_dashboard`, `apps/agent_workflow_framework`) were islands with no shared contract: the dashboard fabricated per-persona numbers via a model-name/round-robin heuristic because `eval.jsonl` and session logs carry no persona field, it knew 5 personas against `config.PERSONA_NAMES`' 6, it parsed `~/.sakthai/` under a *different* env var (`SAKTHAI_DIR` vs `SAKTHAI_HOME`) in two hardcoded places, the Python web API had 3 endpoints and no `serve` command, `apps/` was invisible to ruff/mypy/pytest/coverage and to every CI workflow, and workflow runs landed in a cwd-relative directory nothing else could find. Fixed at the source: persona recorded on `EvalRecord` + session logs (backward-compatible, legacy records surface in an explicit `unattributed` bucket rather than a fake attribution), `web/contracts.py` as the single payload definition with the TypeScript types generated from it and CI failing on drift, `web/api.py` building payloads by reusing the existing parsers, a hybrid `DashboardSource` seam (local fs / HTTP API / demo) that always reports which source it used, workflow runs relocated under `$SAKTHAI_HOME/workflow_runs/`, and a paths-filtered `apps.yml` that finally builds and tests both apps. Plan: [`docs/superpowers/plans/2026-08-26-sak-agent-dashboard-unified.md`](./docs/superpowers/plans/2026-08-26-sak-agent-dashboard-unified.md) · design: [`docs/superpowers/specs/2026-08-26-sak-agent-dashboard-unified-design.md`](./docs/superpowers/specs/2026-08-26-sak-agent-dashboard-unified-design.md) | [/] In progress |
| **Sak Agent Dashboard — full UI pass + Vercel deployment config** — the dashboard had two headers (`layout.tsx` rendered the title and a hardcoded "Runtime Active" badge that was true regardless of whether anything was running; `page.tsx` rendered the same title again), a seven-button tab strip as its only navigation, no headline figures, a centred "Loading…" for every panel, and no state that survived a reload. Replaced with a real app shell: a collapsible sidebar (off-canvas drawer below `lg`), a topbar that names the active section and carries the source badge, auto-refresh and a relative last-updated clock, a six-tile KPI strip with inline SVG sparklines that renders an em dash wherever a figure cannot honestly be computed, a ⌘K command palette, and shaped loading skeletons. Section is now the URL fragment (linkable, back button works); sample-data mode, sidebar collapse and the refresh interval are `localStorage` preferences — both read through `useSyncExternalStore` (`lib/browser-state.ts`) so the server snapshot and the hydrating client agree. Duplication removed where the new chrome already says it: the analytics header's three stat pills and two panel subtitles. Persona cards moved to an auto-fill grid after six fixed columns squeezed the persona name out of its own card. Deployment: `vercel.json` (framework, install command, CSP + `X-Frame-Options`/`Referrer-Policy`/`Permissions-Policy`, `Cache-Control: no-store` over `/api/*`), `.env.example`, and `DEPLOYMENT.md`; an on-page notice now explains an unrequested demo fallback rather than leaving a hosted deploy looking broken. +50 tests (`shell.test.tsx`, `browser-state.test.ts`), 235 passing, lint/build/typecheck clean | [x] 2026-08-27 |
| **Repo-wide file freshness audit** — every top-level doc, config comment and script checked against the code they describe. Verified stale: `CLAUDE.md`/`README.md` claim 14 built-in tools (18), 95 test files / 1,978 tests (107 / ~2,251), nineteen workflows (20, `apps.yml` undocumented), 96.56% coverage (95.85%, below the floor), a three-endpoint web API (nine), and a shared-vs-canonical divergence list that names `telegram/bot.py` (identical now) while omitting `agent/eval.py`, `cli/sessions.py`, `cli/system.py`, `web/server.py`; the `team`/`client` CLI groups, `memory/session_search.py`, `web/api.py`, `web/contracts.py` and `SAKTHAI_CLIENTS_DIR` are undocumented, and `personas/sakthai/skills/.archive/` no longer exists. `docs/capabilities.md` lists 8 of 18 tools and a `sakthai dashboard` Streamlit view removed in v2. `AGENTS.md` still says 85% coverage. `CHANGELOG.md` says the MIT `LICENSE` was removed while a custom all-rights-reserved one exists. Coverage floor asserted at three different values (85 in `AGENTS.md` and `ci.yml`'s comment, 96 in `pyproject.toml`, 96.56% in the docs) against a real 95.85%. Also: `scripts/verify_headers.py` shells out to a removed command and a non-existent path, and `coverage.xml` + `pr_body.txt` are tracked build/scratch artifacts. All of the above corrected; `verify_headers.py` repaired (polls for readiness, cleans up after itself, exits non-zero on a missing header) and verified against both live servers; `coverage.xml` and `pr_body.txt` untracked with `coverage.xml` added to `.gitignore`. Follow-ups landed separately: the four figures re-measured on the merged head (#1213), then the coverage floor made real and the `client/` path convention fixed (see the next row) | [x] 2026-08-28 |
| **Coverage floor made enforcing + `client/` routed through `config.py`** — closes the two items the audit left open. **The floor was decorative:** `pyproject.toml` set `fail_under = 96` but `ci.yml`'s test step concluded success while pytest printed the shortfall (`docs/test-coverage-audit-2026-08-27.md` finding 1), so a real decline produced no signal. Fixed by passing `--cov-fail-under=96` explicitly on the command line rather than relying on the config key, and verified both directions: a deliberately-underfitting subset now exits 1, and the real CI command exits 0. Landing it required clearing the floor first — coverage was 95.86% against 96 — so `team/engine.py` went 86% → 99% (zero missed statements) by testing exactly the gaps the 2026-08-26 and 08-27 audits both named as open: the parallel-batch failure path, its `on_step_start`/`on_step_complete` callbacks, the YAML/missing-file/non-mapping loader branches, adjacent-and-trailing parallel `_group_steps` cases, seeded `initial_context`, and the streaming worker's `pipeline_error` event. Suite 2,275 → 2,305 tests, coverage 95.86% → **96.21%**, 0.21pp of headroom above the floor. **`client/`:** `get_clients_base_dir()` resolved `~/.sakthai/clients` from `Path.home()`, so a persona running under its own `SAKTHAI_HOME` (as `infra/vm-agents/sakthai-agent-run.sh` does for all six) still shared one global clients dir with its siblings. Now defers to a new `config.clients_dir()`; `SAKTHAI_CLIENTS_DIR` still wins, the no-env default is unchanged, and two tests pin both. CLAUDE.md's "no module hard-codes a path" convention is now true rather than aspirational — that was its only exception | [x] 2026-08-28 |
| **Sak Agent Dashboard — keyboard, presentation mode and deploy-failure surfaces** — a follow-on to #1201, built on top of it rather than beside it: the first attempt was branched from a `main` 20 commits stale and duplicated #1201's export, density, digit shortcuts and manifest with a dark-only token layer that would have reverted its light/dark theming; that work was dropped and only what `main` lacked was rebuilt on the current base. **Keyboard:** the command palette now matches on a scored fuzzy subsequence with matched characters underlined (`lib/fuzzy.ts`) instead of a substring, the sidebar tablist is a single tab stop with arrow/Home/End navigation, and the palette gains the focus trap and focus return the drawer already had — `lib/focus.ts` now holds the one definition of "what would Tab reach", which `Drawer.tsx` was carrying inline. **Presentation mode:** a third document-level preference beside theme and density, hiding `[data-chrome]` for a wall display, restored by the same pre-paint bootstrap, left with `Esc`; `@media print` does the same for paper. **Analytics:** a 7d/14d/30d/All trend window carried in the view state, so a link carries it, slicing the series the payload already holds rather than re-requesting. **Deploy:** `/api/health` as an uptime probe that names no path, host or token, `error.tsx` / `global-error.tsx` / `not-found.tsx` / `loading.tsx` so a failed deploy is legible rather than blank, and CORP + `X-DNS-Prefetch-Control` on top of #1201's header set. 366 tests over 13 files (+43), lint/build/typecheck clean, verified in Chromium in both themes | [x] 2026-08-28 |
| **Branch consolidation round 3 — repo reduced to `main` plus live PR heads** — five open PRs triaged by trial-merging each into `main` rather than by their descriptions. Merged: #1214 (SakKing `web/server.py` cookie built from `expected_token`) and, after the fact, #1201. Closed as superseded, each with the evidence on the PR: #1199 (the SakSit copy already carried the fix), #1208 and #1202 (the `aria-label` and Escape-to-close behaviour, and their exact test assertions, already on `main` — the latter via the shared `Drawer` with focus trap and focus return, a superset of the ad-hoc listeners proposed). Both palette branches' only unmerged content was a regression — `eslint ^10.9.1`→`^9.39.5`, `typescript ^7.0.2`→`^5.6.3`, `setup-node` v7.0.0→v6.5.0 — so neither had a merge that would have improved `main`. **#1201 was closed and then reopened**: it held only two `Merge branch 'main'` commits when triaged, but `9283167` (persona filter honest on every panel, 18 files, +606/−56) landed on its branch afterwards, so the close was right for the branch as it stood and wrong for it an hour later; it merged as `63f6b93`, and #1216 — a second PR carrying the same fix, merging to the identical tree — was closed as its duplicate. Follow-on in the same pass (#1217): the `token={val}` cookie sink synced across the three remaining copies (`saksee`, the canonical `sakthai`, `shared`) with the SakKing copy rewrapped so all five are byte-identical, and `.github/workflows/branch-cleanup.yml` added — a dispatchable sweep, dry-run by default, whose eligibility test is `git merge-tree --write-tree` (would merging change a byte?) rather than the ancestor test, which misses precisely the branches this round produced | [x] 2026-08-28 |
| **`Sak-family-auto-cycle` skill rewrite (skill-creator loop)** — the skill never passed `--persona`, so all six dispatches ran as the same default agent (`--provider anthropic`, `claude-opus-4-8`) with no `SOUL.md`, skill overlay, `mcp.json` or per-persona model. Its live-run table (`/opt/data`, `/opt/data/profiles/<name>`) matches no authoritative source — `infra/vm-agents/sakthai-agent-run.sh:28`, the env templates, `CLAUDE.md` and `README.md` all say `$HOME/.sakthai/<persona>`. Verified in-tree: `--persona` with `SAKTHAI_HOME` unset resolves to exactly that production path for all six, collapsing the per-persona table and SakKing's `/opt/data` special case; setting a persona's own home *alongside* `--persona` double-appends the segment (`~/.sakthai/sakthai/sakthai/memory.db`) and fails silently. Also measured: the missing `--no-mcp` costs 71s vs 2s per dry-run dispatch, and `cli/agent.py` raises the credentials error before the unresolved-`--with-skills` one, so a misspelled skill name is masked on a keyless machine — the `[dry-run] skills: N resolved` line is the only reliable signal. Benchmarked against a snapshot of the old version over 3 eval prompts × 11 assertions | [x] 2026-08-28 |
| **Branch consolidation round 4 — repo reduced to `main` plus two superseded heads** — the three open PRs merged in dependency order, each rebased onto the new `main` first because the ruleset requires an up-to-date head: #1223 (CLAUDE.md refreshed against `8e6d785`), #1222 (the `Sak-family-auto-cycle` rewrite above), #1221 (`aria-hidden` on decorative Lucide icons across seven dashboard components). All three auto-deleted on merge. **Not merged:** `palette/aria-label-memory-explorer-tabs-…` and `palette/escape-key-modals-…`, the orphaned heads of #1208 and #1202, closed as superseded in round 3. Re-verified by trial merge rather than by title: both conflict, and their stated fixes are already on `main` — the tablist `aria-label` at `MemoryExplorer.tsx:50`, and Escape-to-close in five components (`Drawer`, `ShortcutsOverlay`, `CommandPalette`, `DisplayMenu`, `PersonaFilter`), a superset of the two ad-hoc listeners proposed. What is left in each diff is purely regressive: it would revert the design-token migration (`text-fg`/`bg-panel` → hardcoded `slate-*`), revert the URL-state drawer and trend-window work, delete ~15 tests, and downgrade `typescript ^6.0.3`→`^5.6.3`, `lucide-react ^1.34.0`→`^1.33.0` and `setup-node` v7.0.0→v6.5.0 | [x] 2026-08-28 |
| **Test coverage audit round 4 (analysis only)** — re-measured at `e10b47a`: **96.21% branch coverage**, 191 uncovered statements / 156 partial branches, 2,305 passed / 8 skipped. The floor landed in `d5fd4d2` and works, so for the first time the series has a gate to reason about. Eight findings in [`docs/test-coverage-audit-2026-08-31.md`](./docs/test-coverage-audit-2026-08-31.md). **Headline (new):** the suite writes outside its sandbox — `tests/conftest.py` has no autouse fixture, and `tests/test_agent_coordinator.py` creates real persona shards at `~/.sakthai/{sakking,saksee}/memory.db`, the exact paths `infra/vm-agents/sakthai-agent-run.sh` gives a deployed persona; `config.persona_memory_db_path()` resolves from `Path.home()` by design, so `SAKTHAI_HOME` cannot contain it. This is also the cause of the ±1-statement flap all three previous audits recorded as unexplained: `store.py:211-217` forks on whether the DB file already exists (pristine `HOME` → 273 miss / 7 partial; second run → 274 / 4). **Also new:** every live-provider smoke test in `test_integration.py` wraps `run_agent` in `except Exception: pytest.skip(...)`, so the repo's only end-to-end provider verification cannot fail; 30 of 168 exception handlers never execute, including **all** of them in `cli/client.py`, `cli/system.py`, `client/manager.py` and `client/verifier.py`; the `web/api.py` ↔ `agent_workflow.persistence` on-disk contract (verified consistent across all eleven keys today) is pinned by no test on either side and degrades silently through `.get()` defaults; and `hypothesis` is used on one module while `guardrails.py` — 98 uncovered units, the largest hole in the repo — is covered by ~600 lines of enumerated attack strings. **Quantified:** headroom is 21 units, against 43 uncovered units for the `client/` subsystem alone; deleting the dead hardened layer takes coverage to 96.43% and headroom to 42, so that decision is not coverage-blocked. **Unrun test files (108) now outnumber run ones (106)** — three tree categories the 28 Aug audit did not count. Carried and re-verified unchanged: the nine env readers (`config.py:296-333`), web-auth query/cookie channels (`web/server.py:213-224`), `web/server.py` persona parity (auth block AST-identical across all five copies), the six can't-fail hardened tests, `guardrails.py` section 6 (`866-910`, entire), `make mutation`'s `|| true`, and `telegram/bot.py`'s `_is_authorized`. `team/engine.py` is closed at 99%. No production code changed | ✅ Done (2026-08-31) |
| **Test coverage audit round 5 (analysis only)** — re-measured at `3e89038`: **96.21% branch coverage**, 191 uncovered statements / 156 partial branches — reproduced to the digit, because the package and test trees are byte-identical to `e10b47a` (everything since landed in `apps/`, docs and workflow config). Five findings in [`docs/test-coverage-audit-2026-09-01.md`](./docs/test-coverage-audit-2026-09-01.md), spent on three questions the series had not asked. **Headline (new): the suite is not hermetic.** `tests/test_cli.py:1397` invokes `run --persona sakking` without `--no-mcp`; `cli/agent.py`'s `_tool_context()` calls `connect_servers()` *before* `run_agent`, so patching `run_agent` does not contain it, and the repo's own `personas/sakking/config/mcp.json` spawns four `npx -y ...@latest` servers against the live npm registry — observed as `npm exec chrome-devtools-mcp@latest` children of pytest. Measured at **4m43s wall / 3.8s CPU for that one test**, ~63% of the 7.5-minute suite, and `connect_servers` fails soft so the test passes identically with the network down: the cost buys no signal. Its three sibling tests at `1460`/`1514` already pass `--no-mcp`. **Corroborated on CI:** the `test (3.11)` job's StepSecurity Harden-Runner egress audit records five `node` connections to `registry.npmjs.org` inside the unit-test job, then the servers reaching `huggingface.co`, `black-forest-labs-flux-1-schnell.hf.space` (SakKing's `hf-media` spec) and `play.googleapis.com` — so the egress is four MCP servers coming up and talking to their backends, on every push. That window is ~16s on a runner against 4m43s in a proxied sandbox, so the wall-time figure is environment-specific and the reason to fix it is the unpinned egress, not the clock. **Also new — mutation spot-check: 3 of 6 mutants survive.** Replacing the Bearer `compare_digest` with a first-8-character prefix match leaves `test_web_auth.py` + `test_web_server.py` green (the only invalid-token test sends `Bearer wrong_token_xyz`, which differs in its first character — no near-miss token is ever tried); flipping `if not expected_token` to fail **open** is caught by nothing (`web/server.py:230` is uncovered); and dropping `","` from `_is_sensitive_path`'s separator tuple survives all 19 guardrail/sentinel files though `src=/etc,dst=/x` is blocked today. Tilde, case and `/tmp` mutants were killed — the corpus works exactly where someone wrote an example. **The sandbox escape is three categories, not one:** a pristine-`HOME` run leaves 2 persona shards **plus 42 session logs and 42 `eval.jsonl` records**, 39 of each from `tests/test_agent_loop.py` alone; `~/.sakthai/sessions/` is what `memory/session_search.py` and the `search_sessions` tool read, so a test run injects fabricated sessions into a real agent's searchable history. Only **9 of 106** test files define an autouse isolation fixture. **Refines 31 Aug finding 1:** the ±1-statement flap that audit attributed to `store.py:211-217`'s create-vs-chmod fork did not reproduce — two back-to-back full runs, pristine `HOME` then the home run 1 had populated, both returned 191/156 and 96.211%, so the drift has another cause; the leak itself is real and larger than reported. **The 96% floor covers ~40% of the repo's test surface:** `apps.yml` runs `vitest run` and `pytest -q` with no coverage measured and no threshold, and PR #1248 rewrote the dashboard (73 src files, 22 components) under no accountability; unrun Python test files are now **111 vs 106** run. Assertion hygiene re-verified good (7 of 1,899 with no failure check, one a fixture). Carried and unchanged: the nine env readers, `test_integration.py`'s unscoped `except`, 30/168 exception handlers, the `web/api.py` on-disk contract, `web/server.py` parity, `guardrails.py` section 6, `telegram/bot.py`, and the dead hardened layer. No production code changed | ✅ Done (2026-09-01) |

## 📋 Sub-Plans

| Plan | Location | Status |
|---|---|---|
| Product & Monetization | [`product/PLAN.md`](./product/PLAN.md) | 🟡 Active — Phase 6 done, extending |
| Security Fixes (2026-07 Audit) | [`security/SECURITY_FIXES_PLAN.md`](./security/SECURITY_FIXES_PLAN.md) | 🔴 HIGH — Credential rotation + Web API auth |
| SakJules — skills organisation | [`personas/sakjules/PLAN.md`](./personas/sakjules/PLAN.md) | ✅ Complete — archived |
| SakTan — daily story & diary | `personas/saktan/PLAN.md` | ⚪ Archived — persona deleted per Beer directive |
| Agent Self-Evolution (×6 agents) | `personas/*/agent-self-evolution/PLAN.md` | 🟡 Active — personalised per agent |
| **Repo Hygiene Round 2** | [SCRATCH_ORGANISATION_PLAN](#scratch-organisation-plan) | 🟡 Active — root cleanup |
| **HF Ecosystem Improvement (19 models · 16 datasets · 7 Spaces)** | [`docs/hf-cards-improvement-plan.md`](./docs/hf-cards-improvement-plan.md) | 🟡 Active — Phase 1 (broken repos) next |
| **Sak Agent Dashboard — unified system** | [`docs/superpowers/plans/2026-08-26-sak-agent-dashboard-unified.md`](./docs/superpowers/plans/2026-08-26-sak-agent-dashboard-unified.md) | 🟡 Active — Phase 0 done, Phase 1 (shared contract) next |

## 🔧 Runtime Notes

Runtime and deployment details are tracked in the implementation docs and the
workspace runtime config under `infra/`.

### Source files

- `docs/agent-diagnosis.md` for the standalone run checklist and runtime notes.
- `infra/hermes-agents/` for the live agent profiles, systemd services, and deployment config.
- `product/TODO.md` for the product delivery checklist.

---

## 🔑 Working Rules

1. **Plan first.** Update this file or the relevant sub-plan before acting.
2. **Surgical edits.** Change only what the task needs; preserve surrounding style.
3. **No duplication.** One source of truth per topic — link, don't copy.
4. **Protect Beer first.** No-cost, low-risk solutions always preferred.

---

## 🧹 SCRATCH_ORGANISATION_PLAN.MD

**Owner:** SakKing (spotter) → SakJules (executor)
**Status:** [x] 2026-08-08 — executed. Superseded in scope by
[`docs/repo-audit-2026-08-08.md`](./docs/repo-audit-2026-08-08.md), which covered
these root files plus generated-state bloat and the `.gitignore` shadowing bug.
**Priority:** Medium

### Problem
Root directory of `Sak-Family-Agent/` has accumulated scratch files that belong in subdirectories:

**5 Python scripts at root** (should be in `scripts/`):
- `_check_models.py` — HF model checker
- `_check_spaces.py` — HF spaces checker
- `_parse_datasets.py` — dataset parser
- `_parse_models.py` — model parser
- `_parse_spaces.py` — spaces parser

**5 JSON data files at root** (should be in `data/`):
- `ci_runs.json` (76 KB) — GitHub Actions run data
- `hf-topics-covered.json` (17 KB) — HF topic coverage data
- `hf_dataset.json` (0 bytes) — empty placeholder
- `hf_ds_size.json` (0 bytes) — empty placeholder
- `hf_embed_check.json` (0 bytes) — empty placeholder

### Steps for SakJules
1. Move 5 Python scripts to `scripts/`
2. Move 2 non-empty JSON files to `data/`
3. Remove 3 empty placeholder JSON files
4. Update any references in scripts that import these files

### Verification
1. `ls *.py *.json` at root → nothing ✅
2. `ls scripts/hf/_*.py` → 5 files ✅ (landed in `scripts/hf/`, not `scripts/`)
3. `ls data/*.json` → `hf-topics-covered.json` ✅ (`ci_runs.json` was deleted, not
   moved: it is a regenerable API-response cache the consuming skill reads from
   `/opt/data/`, never the repo copy)
4. Empty placeholders removed ✅
