# Migrated into the SakThai monorepo

This repository has been **consolidated into the `sakthai-agent-v2` monorepo**.

- **New location:** `infra/hermes-agents/` inside `sakthai-agent-v2`.
- **History:** preserved — this repo's full commit history was merged via
  `git subtree`, so every commit remains reachable in the monorepo.
- **Branch:** the consolidation lives on `claude/monorepo-feasibility-1uzwte`
  in `sakthai-agent-v2`.

New development should happen in the monorepo at `infra/hermes-agents/`. This repository is
retained for reference; it is no longer the source of truth.
