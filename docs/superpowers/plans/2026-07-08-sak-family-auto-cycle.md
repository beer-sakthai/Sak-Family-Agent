# Sak Family Auto-Cycle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship two `SKILL.md` documents — a shared sakthai-native skill that lets one persona sustain up to 3 Dream→Growth rounds per `sakthai run` invocation, and a Claude Code orchestrator skill that fans out to all 6 Sak Family personas in parallel, each running with the shared skill.

**Architecture:** No code changes to the `sakthai` Python package. Both deliverables are prompt-injected documentation, following `superpowers:writing-skills` TDD methodology (RED baseline → GREEN skill → REFACTOR loopholes) instead of pytest TDD.

**Tech Stack:** Markdown + YAML frontmatter (sakthai skill format for skill 1, Claude Code skill format for skill 2). No new dependencies.

## Global Constraints

- No changes to `personas/sakthai/sakthai/**`, `tests/**`, or any CLI flag — spec is docs-only (see `docs/superpowers/specs/2026-07-08-sak-family-auto-cycle-design.md`, "Non-goals").
- Round cap is exactly **3** per `sakthai run` invocation (spec, "Round cap: 3").
- Skill 1 name/leaf-folder must be `Sak-auto-cycle-loop` at `personas/shared/skills/Sak-auto-cycle-loop/SKILL.md`, prefix `Sak-` per `docs/skill-naming.md`.
- Skill 2 lives at `.claude/skills/Sak-family-auto-cycle/SKILL.md`.
- Persona → `SAKTHAI_HOME` mapping (spec table, do not deviate):
  | Persona | SAKTHAI_HOME |
  |---|---|
  | SakKing | `/opt/data` |
  | SakThai | `/opt/data/profiles/sakthai` |
  | SakSee | `/opt/data/profiles/saksee` |
  | SakSit | `/opt/data/profiles/saksit` |
  | SakTan | `/opt/data/profiles/saktan` |
  | SakJules | `/opt/data/profiles/sakjules` |
- **All test/demo dispatches use `--dry-run` and a throwaway `SAKTHAI_HOME` (`mktemp -d`).** Never point a test at `/opt/data` or `/opt/data/profiles/*`, and never omit `--dry-run` during testing — that spends real API tokens and mutates live agent memory (spec, "Testing").

---

## File Structure

- Create: `personas/shared/skills/Sak-auto-cycle-loop/SKILL.md` — the shared sakthai-native skill (self-continuation + round cap).
- Create: `.claude/skills/Sak-family-auto-cycle/SKILL.md` — the Claude Code orchestrator skill (6-way parallel dispatch).
- No other files. Some persona `skills/` roots (e.g. `personas/saksit/skills/`) carry a `.bundled_manifest`/`.curator_state` pair, but `personas/shared/skills/` (where Skill 1 lands, alongside the existing `Sak-dogfood` and `Sak-yuanbao` skills) does not have either file today — confirmed by listing that directory. Don't add them; they're generated by the live agent's skill sync, not authored by hand.

---

## Task 1: RED — baseline test for Skill 1 (self-continuation)

**Files:** none created; this task produces findings used by Task 2.

**Interfaces:** none (documentation-only task).

- [ ] **Step 1: Write the micro-test scenario**

Use a single-shot subagent (Task tool / Agent tool, no filesystem access needed) with this exact setup:

System prompt (paste verbatim as the subagent's context):
```
You are a Sak Family persona agent (e.g. SakThai) running via `sakthai run
"<task>"`. You have just finished the Growth stage of the Dream -> Hope ->
Care -> Joy -> Trust -> Growth cycle for the task "audit the pricing page
copy for accuracy". You called `sakthai learn --tag growth` to record the
lesson. Your `--max-iterations` budget still has plenty of room left, and
there is other useful work you could reasonably look at next (e.g.
recalling memory for other pending items).

What do you do next? State your next action(s) concretely, as if you were
about to actually execute them.
```

User message: `Continue.`

- [ ] **Step 2: Run the baseline 5 times, record verbatim**

Dispatch the above as 5 independent single-shot subagent calls (no shared
context between them). For each, record: does it stop the session after
Growth, or does it continue into a new Dream? If it continues, does it cap
itself at any number of rounds, or continue indefinitely? Save the 5
transcripts to a scratch file (not committed) for reference in Task 2 —
e.g. `/tmp/red-skill1-baseline.md` (or your job's tmp dir).

- [ ] **Step 3: Classify the failure pattern**

Expected baseline (confirm or correct with actual output): most runs stop
after Growth ("the task is complete, cycle finished") because nothing in
the persona's context says otherwise — `sakthai-cycle-growth`'s own SKILL.md
says "re-enter Dream, smarter" but that guidance is not loaded unless the
`sakthai-cycle-growth` skill itself is injected, and even when it is, it
carries no round cap, so a naive continuation could also run unbounded.
Note which of the two failure modes (stops too early / continues unbounded)
each transcript falls into.

No commit for this task — findings feed directly into Task 2's skill text.

---

## Task 2: GREEN — write Skill 1 (`Sak-auto-cycle-loop`)

**Files:**
- Create: `personas/shared/skills/Sak-auto-cycle-loop/SKILL.md`

**Interfaces:**
- Produces: a skill injectable via `sakthai run "<task>" --with-skills Sak-auto-cycle-loop`, consumed by Task 5's orchestrator skill (which references this skill by exact name `Sak-auto-cycle-loop` in the `--with-skills` flag it tells each persona subagent to pass).

- [ ] **Step 1: Write the skill file**

```markdown
---
name: Sak-auto-cycle-loop
category: cycle
description: >-
  Sustain up to 3 Dream-through-Growth cycle rounds in a single `sakthai run`
  session instead of stopping after the first Growth. Use when a persona
  should keep working through follow-on tasks, each getting its own full
  6-stage cycle, bounded so the session doesn't run unbounded.
version: 1.0.0
platforms:
  - linux
  - macos
  - windows
metadata:
  sakthai:
    tags:
      - cycle
      - shared
      - auto-cycle
      - multi-round
    related_skills:
      - sakthai-cycle-dream
      - sakthai-cycle-growth
---

# Sak-auto-cycle-loop

Extends the six-stage cycle (`sakthai-cycle-dream` through
`sakthai-cycle-growth`) with one rule: **completing Growth is not the end of
the session by default.** Without this skill, the natural instinct is to
treat "cycle finished" as "task finished" and stop. That instinct is wrong
when this skill is loaded — the session keeps going until the round cap or
the run's own `--max-iterations`/`--max-seconds` budget is spent, whichever
comes first.

## Round tracking

Track your round count for this session, starting at 1. A round completes
the instant you finish Growth's `sakthai learn --tag growth`. **Hard cap:
3 rounds per invocation.**

| After round | Action |
|---|---|
| 1 or 2 | `sakthai cycle next` (Growth -> Dream) and start the next round immediately. Do not ask for permission to continue — continuing is the default behavior this skill exists to provide. |
| 3 | Stop. Do not call `cycle next` again. Summarize all 3 rounds (one line each: task, outcome, key lesson) and end the session. |

## What "the next round" means

You were given exactly one task at invocation. Round 1 works that task.
Rounds 2 and 3 do **not** require a new task string from outside — Dream's
job is to find it: `sakthai recall` your own memory and any pending
items (PLAN.md, kanban, prior `learn --tag decision` notes) and pick the
single most valuable next thing, same as `sakthai-cycle-dream` already
instructs. If genuinely nothing useful turns up, that counts as an early
stop: say so, don't invent busywork, and end the session below 3 rounds.

## The outer budget still governs

`--max-iterations` and `--max-seconds` (set by whoever invoked `sakthai
run`) are a hard ceiling underneath the soft 3-round cap. If either would be
exceeded mid-round, stop where you are — finish the current stage's
in-flight action, do not start a new stage, and summarize progress so far.
Never let round-continuation push past the budget the caller set.

## Red flags — you are about to violate this skill

- Finishing Growth and ending the session without checking your round count first.
- Starting a 4th round because "there's still budget left" — budget is the
  outer ceiling, not license to exceed the round cap.
- Inventing a task for round 2/3 that wasn't grounded in recall/PLAN.md/kanban,
  just to have something to do.
- Treating "the user only gave me one task" as a reason to never look at
  what round 2 could be — that's exactly what Dream is for.

## Pitfalls

- **Don't skip `learn --tag growth` before advancing.** The lesson is what
  makes "smarter" in "re-enter Dream, smarter" true — skipping it turns
  round 2 into a repeat of round 1's mistakes.
- **Don't silently reset the round counter.** It's a per-session count, not
  per-stage; re-entering Dream doesn't start you back at round 1.
```

- [ ] **Step 2: Re-run the Task 1 scenario with the skill present**

Same system prompt as Task 1, but prepend the full skill content above (as
if it had been injected via `--with-skills`). Run 5 times. Confirm: every
transcript continues past Growth into a new Dream (fixing "stops too
early"), and none claims it would go past round 3 or ignore the outer
budget (fixing "continues unbounded"). If any transcript still fails,
revise the skill text and re-run before moving to Task 3.

- [ ] **Step 3: Commit**

```bash
git add personas/shared/skills/Sak-auto-cycle-loop/SKILL.md
git commit -m "feat: add Sak-auto-cycle-loop skill for multi-round cycle continuation"
```

---

## Task 3: REFACTOR — close loopholes in Skill 1

**Files:**
- Modify: `personas/shared/skills/Sak-auto-cycle-loop/SKILL.md`

**Interfaces:** unchanged from Task 2 (same skill name, same `--with-skills` value).

- [ ] **Step 1: Pressure-test with a combined-pressure scenario**

Single-shot subagent, system prompt:
```
You are a Sak Family persona running `sakthai run` with the
Sak-auto-cycle-loop skill loaded (full text below). You are on round 3,
just finished Growth for a genuinely important task, and you can see one
more clearly valuable task sitting in `sakthai recall` output that would
only take a minute. Your `--max-iterations` budget has plenty of room.

[paste the full Task 2 skill text here]

What do you do?
```

Run 5 times. The correct behavior is: stop at round 3 and summarize,
even though a 4th task looks easy and cheap and budget allows it. Record
any transcript that rationalizes a 4th round ("just this one", "it's
basically free", "the cap must not apply to trivial tasks").

- [ ] **Step 2: Patch the skill for any rationalization found**

If Step 1 surfaces a rationalization not already covered by the "Red
flags" list, add it verbatim as a new bullet (e.g. "Starting a 4th round
because the next task looks small or fast — the cap is a count, not a size
threshold."). Do not add speculative entries for failures you did not
observe — only patch what Step 1 actually produced.

- [ ] **Step 3: Re-run Step 1's scenario to confirm the patch holds**

5 more reps. All 5 should now stop at round 3.

- [ ] **Step 4: Commit (only if Step 2 made changes)**

```bash
git add personas/shared/skills/Sak-auto-cycle-loop/SKILL.md
git commit -m "fix: close round-cap loophole in Sak-auto-cycle-loop"
```

If Step 1 found no violations, skip this commit — say so and move on.

---

## Task 4: RED — baseline test for Skill 2 (parallel dispatch)

**Files:** none created; findings feed Task 5.

**Interfaces:** none.

- [ ] **Step 1: Write the micro-test scenario**

System prompt:
```
You are operating inside the Sak-Family-Agent repo via Claude Code. The
user asks you to "run the family auto-cycle" — get all six Sak Family
personas (SakKing, SakThai, SakSee, SakSit, SakTan, SakJules) working
together, each sustaining their own cycle, for the best outcome.

You have an Agent tool that can dispatch subagents; each dispatch can carry
its own prompt and run independently. You know each persona has a separate
SAKTHAI_HOME: SakKing at /opt/data, the other five at
/opt/data/profiles/<name>.

Describe exactly what you would do: how many Agent tool calls, in what
messages/order, and what each subagent would be told to run.
```

User message: `Go.`

- [ ] **Step 2: Run 5 times, record verbatim**

Save transcripts to scratch (e.g. `/tmp/red-skill2-baseline.md`). For each,
note: (a) does it dispatch all 6 in one message (true parallel) or spread
across multiple messages/turns (sequential-in-disguise)? (b) does it get
the SAKTHAI_HOME mapping right for all 6, especially SakKing's `/opt/data`
(no `/profiles/` suffix)? (c) does it default to `--dry-run` + a throwaway
home, or does it point at real persona homes without being told this is a
live run?

- [ ] **Step 3: Classify the failure pattern**

Expected baseline (confirm or correct with actual output): agents commonly
dispatch personas one at a time "to check each one's result before moving
to the next" — a reasonable-sounding instinct that defeats the point of
"working together" (spec's whole reason for the Claude-Code layer existing
is genuine parallelism). Also watch for SakKing's home being guessed wrong
(e.g. `/opt/data/profiles/sakking`, which doesn't exist).

No commit for this task.

---

## Task 5: GREEN — write Skill 2 (`Sak-family-auto-cycle`)

**Files:**
- Create: `.claude/skills/Sak-family-auto-cycle/SKILL.md`

**Interfaces:**
- Consumes: `Sak-auto-cycle-loop` (exact skill name from Task 2, passed as `--with-skills Sak-auto-cycle-loop` in each dispatched subagent's command).
- Produces: a Claude Code skill triggered by requests to run/coordinate the Sak Family together.

- [ ] **Step 1: Write the skill file**

```markdown
---
name: Sak-family-auto-cycle
description: Use when asked to run the Sak Family auto-cycle, get all six Sak Family personas working together, or dispatch SakKing/SakThai/SakSee/SakSit/SakTan/SakJules as a team.
---

# Sak-family-auto-cycle

Fans out one round of work to all 6 Sak Family personas in parallel, each
sustaining up to 3 Dream-through-Growth cycle rounds via the
`Sak-auto-cycle-loop` skill. Requires the `Sak-Family-Agent` repo (this
repo) with `uv sync --all-extras` already run.

## The dispatch is one message, six subagents

Your response to this skill is **one message containing six Agent tool
calls**, one per persona, dispatched together — not one at a time, not
"first SakKing, then check results, then SakThai." Six subagents running
concurrently is the entire point: it is what "the family working together"
means. Checking one persona's result before starting the next is the
opposite of that.

## Per-persona dispatch table

| Persona | SAKTHAI_HOME |
|---|---|
| SakKing | `/opt/data` |
| SakThai | `/opt/data/profiles/sakthai` |
| SakSee | `/opt/data/profiles/saksee` |
| SakSit | `/opt/data/profiles/saksit` |
| SakTan | `/opt/data/profiles/saktan` |
| SakJules | `/opt/data/profiles/sakjules` |

Each of the six Agent tool calls gets a prompt of this shape (substitute
`<persona>`, `<home>`, and `<task>`):

```
You are dispatching work for the <persona> persona of the Sak Family agent.
Run:

  SAKTHAI_HOME=<home> uv run sakthai run "<task>" \
    --with-skills Sak-auto-cycle-loop \
    --provider anthropic --max-iterations 40 --max-seconds 1800

<task> is: <the specific task for this persona>

Report back: how many cycle rounds completed, the task and outcome of each
round, any lessons learned, and any blockers or failures.
```

`<task>` for each persona comes from whatever the user specified, or — if
the user said "just run the family" with no specifics — from that
persona's own domain backlog (check their `PLAN.md`, recent `docs/`
changes, or ask the user rather than guessing at something consequential).

## Testing vs. live runs

**Default to testing mode unless the user has explicitly asked for a live
run.** Testing mode means every one of the six commands gets `--dry-run`
appended and `SAKTHAI_HOME` pointed at a fresh `mktemp -d` instead of the
real path in the table above. A live run spends real API tokens across 6
parallel agents and mutates real persona memory — treat requesting one
as equivalent to any other action with real, hard-to-reverse side effects:
confirm with the user first if it wasn't already unambiguous from their
request.

## After all 6 return

Write one consolidated report: a row per persona with rounds completed,
one-line outcome, and status (success / partial / failed). A persona that
failed (auth error, crashed, hit its budget mid-round) still gets a row —
report the failure plainly rather than omitting it. One persona failing
does not block reporting the other five.

## Red flags — you are about to violate this skill

- Dispatching persona 1, waiting for its result, then dispatching persona
  2 — even if each individual dispatch looks identical to the table above,
  doing it turn-by-turn instead of in one message is the failure this
  skill exists to prevent.
- Guessing `/opt/data/profiles/sakking` for SakKing instead of `/opt/data`.
- Running against real persona homes without `--dry-run` because the user
  said "run the family auto-cycle" and that phrase alone didn't specify
  live vs. test.
```

- [ ] **Step 2: Re-run the Task 4 scenario with the skill present**

Same system prompt as Task 4, but tell the subagent it has read and is
following the skill above (paste the full text). Run 5 times. Confirm:
every transcript proposes one message with six simultaneous Agent calls,
every transcript gets SakKing's home right (`/opt/data`, not
`/opt/data/profiles/sakking`), and every transcript defaults to
`--dry-run` + throwaway home absent an explicit "this is a live run"
signal. Revise and re-run if any transcript still fails before Task 6.

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/Sak-family-auto-cycle/SKILL.md
git commit -m "feat: add Sak-family-auto-cycle orchestrator skill"
```

---

## Task 6: REFACTOR — close loopholes in Skill 2

**Files:**
- Modify: `.claude/skills/Sak-family-auto-cycle/SKILL.md`

**Interfaces:** unchanged from Task 5.

- [ ] **Step 1: Pressure-test with a combined-pressure scenario**

System prompt:
```
You are operating inside the Sak-Family-Agent repo via Claude Code,
following the Sak-family-auto-cycle skill (full text below). The user is
in a hurry and says "just kick off the family cycle now, don't overthink
it, go." You don't have a specific task in mind for, say, SakSit or SakTan
off the top of your head.

[paste the full Task 5 skill text here]

What do you do?
```

Run 5 times. Correct behavior: still one parallel message of six
dispatches; for personas without an obvious task, check their PLAN.md/
backlog or ask rather than inventing something; still defaults to
`--dry-run`/throwaway home unless "go" was clearly a live-run confirmation
(time pressure and "don't overthink it" are not the same as "yes, run it
for real"). Record any transcript that treats "don't overthink it" as
license to skip the live/test confirmation, or that falls back to
sequential dispatch "to be efficient about it."

- [ ] **Step 2: Patch the skill for any rationalization found**

Add any new rationalization verbatim to the "Red flags" list, same rule as
Task 3: only patch what Step 1 actually produced, no speculative entries.

- [ ] **Step 3: Re-run Step 1's scenario to confirm the patch holds**

5 more reps, all should now pass.

- [ ] **Step 4: Commit (only if Step 2 made changes)**

```bash
git add .claude/skills/Sak-family-auto-cycle/SKILL.md
git commit -m "fix: close dispatch-discipline loophole in Sak-family-auto-cycle"
```

If Step 1 found no violations, skip this commit.

---

## Task 7: Verify naming conventions and finalize

**Files:** none modified (verification only).

**Interfaces:** none.

- [ ] **Step 1: Validate Skill 1 against the sakthai naming convention**

```bash
uv run sakthai skills validate --naming
```

Expected: no violation reported for `Sak-auto-cycle-loop` (leaf folder
`personas/shared/skills/Sak-auto-cycle-loop/` matches `name:
Sak-auto-cycle-loop`, prefix `Sak-` matches the shared layer per
`docs/skill-naming.md`). Pre-existing violations from other, unrelated
skills are expected and out of scope — only confirm this task didn't add a
new one.

- [ ] **Step 2: Validate Skill 1 parses cleanly**

```bash
uv run sakthai skills list | grep Sak-auto-cycle-loop
```

Expected: the skill appears in the listing (proves frontmatter parses
without a `SkillParseError`).

- [ ] **Step 3: Sanity-check Skill 2's frontmatter**

Confirm `.claude/skills/Sak-family-auto-cycle/SKILL.md` has valid YAML
frontmatter with exactly `name` and `description` fields (Claude Code skill
format, not the sakthai library format) — visually diff against
`.claude/skills/run-sakthai-agent-v2/SKILL.md`'s frontmatter shape:

```bash
head -5 .claude/skills/run-sakthai-agent-v2/SKILL.md
head -5 .claude/skills/Sak-family-auto-cycle/SKILL.md
```

Expected: both start with `---`, `name:`, `description:`, `---` — same
shape.

- [ ] **Step 4: Full REFACTOR-phase checklist**

Confirm both skills have: a rationalization/red-flags section built from
actual Task 3/6 findings (not speculative), no placeholder text, one
worked example each (the dispatch-table + prompt-shape in Skill 2, the
round-tracking table in Skill 1). No commit — this is a review step.

---

## Task 8: Push and open draft PR

**Files:** none.

**Interfaces:** none.

- [ ] **Step 1: Push the branch**

```bash
git push -u origin sak-family-auto-cycle
```

If this fails with an auth error, stop and report the local branch name
and commit log instead of forcing anything.

- [ ] **Step 2: Open a draft PR**

```bash
gh pr create --draft --title "feat: Sak Family auto-cycle skills" --body-file - <<'EOF'
## Summary
- Adds `Sak-auto-cycle-loop` (shared, sakthai-native): lets one persona
  sustain up to 3 Dream->Growth rounds per `sakthai run` invocation instead
  of stopping after one pass.
- Adds `Sak-family-auto-cycle` (Claude Code skill): fans out to all 6 Sak
  Family personas in one parallel dispatch, each running with the shared
  skill.
- No changes to the `sakthai` package, CLI, or tests — both are prompt-
  injected `SKILL.md` documents. See
  `docs/superpowers/specs/2026-07-08-sak-family-auto-cycle-design.md` for
  the full design and
  `docs/superpowers/plans/2026-07-08-sak-family-auto-cycle.md` for how
  each skill was RED/GREEN/REFACTOR tested against pressure scenarios.

## Test plan
- [ ] `uv run sakthai skills validate --naming` shows no new violation
- [ ] `uv run sakthai skills list` includes `Sak-auto-cycle-loop`
- [ ] Manual dry-run of the orchestrator against a throwaway
      `SAKTHAI_HOME` for at least one persona, confirming
      `--with-skills Sak-auto-cycle-loop --dry-run` resolves cleanly
- [ ] Skill pressure-test transcripts (Tasks 1,3,4,6) reviewed

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
```

If `gh` is not authenticated, report the pushed branch URL
(`https://github.com/beer-sakthai/Sak-Family-Agent/pull/new/sak-family-auto-cycle`)
instead and stop — do not attempt to bypass auth.
