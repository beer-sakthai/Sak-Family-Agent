# Cron Job: Self-Heal Watchdog

**Job ID:** 5ec81087718a
**Run Time:** 2026-07-23 14:42:14
**Schedule:** every 1m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are SakThai running the Cron Watchdog Self-Heal.

Check all cron jobs for issues and heal any that are down. Report only when you find something to fix — stay silent if all is well.

Load the `cron-watchdog-self-heal` skill with `skill_view(name='cron-watchdog-self-heal')` and follow its procedure:

1. List all jobs via `cronjob(action='list')`
2. For each recurring job (repeat: forever), check if enabled: false or state: completed
3. If found, re-enable by updating the job with the same config and enabled: true
4. Report only if you healed something — one line per healed job. Stay silent otherwise.

## Response

[SILENT]
