# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtualenvs
.venv/
venv/
env/

# Node / dashboard build deps (the dashboard has its own .gitignore too)
node_modules/
# Stale duplicate of dashboard/ — kept only locally, never committed
web-dashboard/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.json
htmlcov/

# Local config / secrets
.env
.env.local

# Editor
.idea/
.vscode/
*.swp

# Persona skill runtime caches/state (regenerated; not authored content)
personas/*/skills/.hub/
personas/*/skills/.curator_state
personas/*/skills/.usage.json
personas/*/skills/.bundled_manifest

# Local scratch / build artifacts (never commit these)
/gitleaks
*.patch
changes.txt

# mutmut mutation-testing working copy + caches (regenerated by `mutmut run`)
/mutants/
.mutmut-cache
mutmut-cache

# Local-only Antigravity IDE scratch (untracked; never commit)
oh-my-antigravity/

# Hypothesis test database (local cache, must not be committed)
.hypothesis/

.worktrees/

# Temp scripts for consolidation
copy_skills.sh
fix_missing.sh
gen_batch.sh
gen_tool_call.sh
process_batch.sh

# Temp scripts
copy_skills.sh
fix_missing.sh
gen_batch.sh
gen_tool_call.sh
process_batch.sh
gen_batch_json.py
gen_tool.py

# Secrets — never commit OAuth/credential caches (added after security review)
.jules/cache/
**/oauth_creds.json
**/*_creds.json

# Security-audit scratch (Gemini SAST working notes)
.gemini_security/

# Test/build artifacts (kept local only)
coverage.xml
sakking-dashboard.tar.gz
