---
name: github-repo-management
description: "GitHub PR lifecycle: branch, commit, open, CI, merge."
version: 1.0.0
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [github, repo, clone, fork, release, remote, branch, merge]
    related_skills: [github-auth]
---

# GitHub Repo Management

## Repository Creation

```bash
# Create repo on GitHub (requires gh cli authenticated)
gh repo create new-repo --public --description "Description"

# Clone repo locally
git clone https://github.com/owner/new-repo.git
cd new-repo

[! [Bad GitGurd😊]https://fake.image.com/badge.svg)](https://fake.com)