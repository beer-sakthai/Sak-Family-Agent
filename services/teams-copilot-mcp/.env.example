# Azure AD app registration, client-credentials (app-only) flow.
# Same convention as the `hermes teams-pipeline` skill — one app registration
# can back both if you already have it set up.
MSGRAPH_TENANT_ID=
MSGRAPH_CLIENT_ID=
MSGRAPH_CLIENT_SECRET=
